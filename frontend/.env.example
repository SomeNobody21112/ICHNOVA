# Google Identity Services OAuth client ID (Web application type).
# Authorised JavaScript origins must include the URL the app is served from,
# e.g. http://localhost:5173 and http://127.0.0.1:8765
VITE_GOOGLE_CLIENT_ID=
